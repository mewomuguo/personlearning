# 自然科「我的理由」更新 — 2026-09-18

- 基於 GitHub mewomuguo/personlearning 的 d55c5c0 版本。
- 「我的理由」維持選填，上限 1000 字；按「確認答案」後隨該次作答保存。
- 理由不判定正誤，不影響題目分數、總分、XP、印章或復習判定。
- 自然科「下載自然科學習紀錄」包含理由、時間、回合、單元、題號、版本及題目文字。
- 新紀錄保存當時的題目文字及單元名稱，日後題庫改版仍可辨識紀錄。
- 資料保險箱的 JSON 匯出／複製／匯入還原包含理由。
- 舊存檔正常讀取，報告標示「舊紀錄未保存理由」；新作答留空則標示「未填寫」。舊版未保存的文字無法補回。
- 沿用單一瀏覽器存檔，現有網站沒有學生帳號或姓名欄位；不新增多人身分系統。
- 尚未按「確認答案」的理由不保存，畫面已明示。

## 驗證

執行 `node --test tests/science-reason.test.cjs`，7 項全數通過：
保存／重新載入與備份還原、文字報告與歷史題目、空白及舊存檔、分數與 XP／印章不變、異常資料拒絕、重複提交、UI 提交與換題清空。
保存測試使用記憶體 localStorage 替身；UI 測試使用 React 元件及 hook 替身，未取代實際瀏覽器驗證。
JavaScript 語法、相對資源引用與壓縮檔完整性另行檢查。
瀏覽器工具無法完成管理員安全政策驗證，故未完成實際畫面與跨瀏覽器驗證。
